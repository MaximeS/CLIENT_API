'use strict';

var rabbit = require('rabbit.js');

var context = rabbit.createContext();
var socket = context.socket('WORKER');

socket.connect('sails.models.message.persistence', function (err) {
  console.log('socket connected');
});